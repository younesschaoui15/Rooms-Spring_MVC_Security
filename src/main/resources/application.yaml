server:
  port: 8080

spring:
  application:
    name: Rooms - Spring MVC Security

  profiles:
    active: dev

  mvc:
    apiversion:
      use:
        #API path segment index
        path-segment: 1


